# Copy to .env.local and fill in. Never commit real values: secrets stay empty here.
# The anonymous tool (build, key, zip, torrent) runs without any of these.
# Accounts, saved packs and the public pack list need the first five.

# MongoDB connection string, like mongodb+srv://user:password@cluster.example.net. The app uses the "packs" database.
MONGODB_URI=

# better-auth session secret, at least 32 characters. Generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Base URL better-auth builds its callback URLs from.
BETTER_AUTH_URL=http://localhost:3000

# osu! OAuth app (https://osu.ppy.sh/home/account/edit#oauth), callback {BETTER_AUTH_URL}/api/auth/callback/osu.
OSU_CLIENT_ID=12345
OSU_CLIENT_SECRET=

# Optional. osu! user ids allowed into /admin, comma-separated, like 1234567,7654321. Empty means no admins.
ADMIN_OSU_IDS=

# Optional, CI only. Builds without real secrets by using placeholders nothing connects with.
# SKIP_ENV_VALIDATION=true
